<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
              http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="order-service-provider"  />

    <!-- 使用zookeeper注册中心暴露服务地址 -->
    <dubbo:registry address="zookeeper://localhost:2181" />

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20880" />

    <!-- 声明需要暴露的服务接口 -->
    <!-- timeout:超时时间;毫秒-->
    <!--retries:重试次数，默认值为2，不包含第一次调用，实际调用次数为retries+1
        冪等服务建议重试
        非冪等服务不建议重试（设置retries为0即可）
     -->
    <!-- version:版本号-->
    <dubbo:service interface="com.atguigu.api.UserService" ref="userService" version="1.0.0"  timeout="5000" retries="3">
       <!-- <dubbo:method name="users" timeout="1000"></dubbo:method>-->
    </dubbo:service>

    <!-- 新版本 -->
    <dubbo:service interface="com.atguigu.api.UserService" ref="userService2" version="2.0.0"  timeout="5000" retries="3"></dubbo:service>

    <!-- 和本地bean一样实现服务 -->
    <bean id="userService" class="com.atguigu.provider.UserServiceImpl01" />
    <bean id="userService2" class="com.atguigu.provider.UserServiceImpl02" />

    <!-- 超时配置测试
           规则：
                1、方法级优先，接口级次之，全局配置再次之。
                2、如果级别一样，则消费方优先，提供方次之。
    -->
    <dubbo:provider timeout="5000"></dubbo:provider>
</beans>